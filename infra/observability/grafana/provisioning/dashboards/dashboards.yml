apiVersion: 1

providers:
  - name: arquivo-pipeline
    orgId: 1
    folder: Arquivo
    type: file
    disableDeletion: false
    updateIntervalSeconds: 30
    options:
      path: /var/lib/grafana/dashboards
