# =============================================================================
# Arquivo da Violência - Staging Environment Configuration
# =============================================================================
# Copy this file to .env.staging and fill in your values.
# These variables are used by docker-compose.staging.yml
#
# Note: Production variables from .env are used as fallbacks where applicable.
# =============================================================================

# -----------------------------------------------------------------------------
# Staging-Specific API Keys
# -----------------------------------------------------------------------------
# You can use the same key as production, or a separate one for staging
STAGING_GEMINI_API_KEY=your-staging-gemini-api-key

# -----------------------------------------------------------------------------
# Staging Authentication
# -----------------------------------------------------------------------------
# Use different credentials for staging to avoid confusion
STAGING_JWT_SECRET_KEY=staging-secret-key-change-me
STAGING_ADMIN_USERNAME=admin
STAGING_ADMIN_PASSWORD=staging-admin-password

# -----------------------------------------------------------------------------
# Staging Pipeline Settings
# -----------------------------------------------------------------------------
# Usually disabled on staging to avoid duplicate processing
STAGING_ENABLE_CRON=false

# -----------------------------------------------------------------------------
# Staging Telegram Notifications (Optional)
# -----------------------------------------------------------------------------
# You may want to use a different chat for staging notifications
# or leave empty to disable staging notifications
STAGING_TELEGRAM_BOT_TOKEN=
STAGING_TELEGRAM_CHAT_ID=

# =============================================================================
# Production Variables (also needed in .env for staging to work correctly)
# =============================================================================
# The following are inherited from the base docker-compose.yml:
#
# GEMINI_API_KEY - Falls back if STAGING_GEMINI_API_KEY not set
# JWT_SECRET_KEY - Production value (staging uses STAGING_JWT_SECRET_KEY)
# TELEGRAM_BOT_TOKEN - Production notifications
# TELEGRAM_CHAT_ID - Production chat

